// Auth placeholder — replace with Clerk or NextAuth when adding auth
// Example: import { clerkMiddleware } from "@clerk/nextjs/server"

export function getCurrentUserId(): string | null {
  return null;
}
