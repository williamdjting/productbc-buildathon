// DB placeholder — replace with Prisma when adding persistence
// Example:
//   import { PrismaClient } from "@prisma/client"
//   const prisma = new PrismaClient()
//   export default prisma

export const db = null;
